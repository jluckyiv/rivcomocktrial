# direnv config — auto-loads .env.local on cd into this repo.
# Install direnv (https://direnv.net/) and run `direnv allow` once after clone.
# Without direnv, source .env.local manually or rely on per-script handling.
dotenv_if_exists .env.local
