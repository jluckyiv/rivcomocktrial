# Single source of truth for the isolated test environment.
# Read by docker-compose.test.yml AND by npm test:* scripts.
# These are local-only test credentials — safe to commit.
PB_URL=http://localhost:28090
PB_ADMIN_EMAIL=test-admin@test.invalid
PB_ADMIN_PASSWORD=testpass1234
