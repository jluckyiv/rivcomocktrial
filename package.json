{
  "name": "rivcomocktrial",
  "private": true,
  "scripts": {
    "dev": "echo 'Run in separate terminals:\n  npm run pb:dev\n  npm run fe:dev'",
    "pb:start": "docker compose up -d",
    "pb:stop": "docker compose down",
    "pb:dev": "docker compose watch",
    "pb:kill": "pkill -f 'docker compose watch' || true; docker compose down",
    "fe:dev": "cd frontend && PATH=\"./node_modules/.bin:$PATH\" elm-land server",
    "fe:build": "cd frontend && PATH=\"./node_modules/.bin:$PATH\" elm-land build",
    "fe:review": "cd frontend && npx elm-review",
    "fe:test": "cd frontend && npx elm-test",
    "e2e": "PB_ADMIN_EMAIL=$(op read 'op://Private/localhost dev pocketbase admin user/Email') PB_ADMIN_PASSWORD=$(op read 'op://Private/localhost dev pocketbase admin user/Password') npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
